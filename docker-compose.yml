services:
  lifescape:
    build:
      context: .
      target: production
    container_name: lifescape
    restart: unless-stopped
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=production
    command: npm run preview -- --host